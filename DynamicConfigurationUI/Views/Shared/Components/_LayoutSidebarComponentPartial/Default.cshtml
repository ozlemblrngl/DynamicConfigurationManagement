@model IEnumerable<ConfigurationListDto>

<div class="sidebar">
	<div class="scrollbar-inner sidebar-wrapper">
		<ul class="nav">
			<div class="user">
				<div class="info container-fluid d-flex justify-content-center">
						<a class="" aria-expanded="true">
						<span>
							<b>APPLICATIONS</b>
							</span>
						</a>
				</div>
			</div>

				@foreach (var item in Model)
				{
				<li class="container-fluid d-flex justify-content-center">
					@Html.ActionLink(item.ApplicationName, "GetByApplicationName", "Configuration", new { applicationName = item.ApplicationName }, null)
				</li>
				}
		 </ul>
	</div>
</div>
